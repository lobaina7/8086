;====================================================================
; Main.asm file generated by New Project wizard
;
.MODEL	SMALL
.8086
.stack 100h
.code
org 400h
mov ax,0042h
mov ds,ax

jmp start

start: mov bx, offset temp
mov cx,720
XMED: mov al,[bx]
      add temp_prom,ax
      inc bx
      loop XMED
xor dx,dx
mov ax,temp_prom
mov bx,720
div bx
mov temp_prom,ax
end1:
	jmp   end1
.data

temp db 720 dup(37)
temp_prom dw 0
END
